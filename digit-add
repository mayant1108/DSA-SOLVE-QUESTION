#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int N, sum = 0;
        cin >> N;

        while (N) {
            int digit = N % 10; 
            sum += digit;      
        }

        cout << sum << endl;
    }

    return 0;
}
